<div class="pagebanner" style="background-image: linear-gradient(-225deg, rgba(38,208,206,0.5) 0%, rgba(26,41,128,0.5) 50%), url('<%= cl_image_path 'bannerfont_dm4pkp', crop: :fill %>');">
      <h1 style= "background-color: rgba(38,208,206,0.5);">Your bookings</h1>
      <p style= "background-color: rgba(26,41,128,0.5);">Can't remember what you've book ? Here is a reminder, little piece of shit.</p>
</div>

<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3">
     <div class="text-center">
    <% @bookings.each do |bk| %>
       <p style="font-size: 2vh;"><%= link_to "Your booking n°#{bk.id} (#{bk.meeting_room.name} on #{bk.day})" , booking_path(bk) %></p>
    <% end %>
      </div>
    </div>
  </div>
  </div>


